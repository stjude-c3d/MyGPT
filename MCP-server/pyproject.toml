[project]
name = "mygpt-backend"
version = "0.1.0"
description = "MCP server for MyGPT"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.8.1",
    "fastmcp>=2.3.0",
    "httpx>=0.28.1",
]

[project.scripts]
mygpt = "mygpt_backend:main"